{
  "name": "@cctc-ext/cu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Unleashed fleet board — CCTC disk extension driving the local `claude-unleashed` daemon via brokered exec.",
  "scripts": {
    "build:renderer": "BUILD_TARGET=renderer vite build",
    "build:main": "BUILD_TARGET=main vite build",
    "build": "npm run build:renderer && npm run build:main",
    "package": "node scripts/package.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "react": "^18"
  }
}
